name: Speed Build Maintenance

on:
  workflow_dispatch:

jobs:
  maintenance:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Speed build workflow is being replaced by the current stability build."
