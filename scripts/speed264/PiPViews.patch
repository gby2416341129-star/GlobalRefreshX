--- /mnt/data/speed264_src/pip_swift/pip_swift/PiPViews.swift	2026-09-10 08:17:20.000000000 +0000
+++ /mnt/data/speed264_mod/pip_swift/pip_swift/PiPViews.swift	2026-09-10 08:44:00.495392835 +0000
@@ -287,7 +287,7 @@
                         .font(.headline)
                     Text("CADisplayLink")
                         .font(.subheadline.weight(.semibold))
-                    Text(L10n.text("2.6.3 已隔离 UIUpdateLink / Hybrid / 立即呈现实验，避免已知的运行时闪退。后续只会在独立实验构建中重新开放。", "2.6.3 quarantines UIUpdateLink / Hybrid / immediate-presentation experiments after the known runtime crash. They will return only in isolated experimental builds."))
+                    Text(L10n.text("2.6.4 已隔离 UIUpdateLink / Hybrid / 立即呈现实验，避免已知的运行时闪退。后续只会在独立实验构建中重新开放。", "2.6.4 quarantines UIUpdateLink / Hybrid / immediate-presentation experiments after the known runtime crash. They will return only in isolated experimental builds."))
                         .font(.caption)
                         .foregroundStyle(.secondary)
                 }
@@ -396,12 +396,19 @@
 
                 HStack(spacing: 10) {
                     Circle()
-                        .fill(isPiPActive ? Color.green : Color.secondary.opacity(0.55))
+                        .fill(presentationState.activity == .paused ? Color.orange : (isPiPActive ? Color.green : Color.secondary.opacity(0.55)))
                         .frame(width: 8, height: 8)
                     Text(localizedStatusTitle)
                         .font(.headline)
                     Spacer()
                 }
+
+                if presentationState.activity == .paused, let pauseReason = presentationState.pauseReason {
+                    Text(pauseReason.detail)
+                        .font(.caption)
+                        .foregroundStyle(.secondary)
+                        .fixedSize(horizontal: false, vertical: true)
+                }
             }
         }
         .accessibilityElement(children: .combine)
@@ -613,6 +620,7 @@
         case .ready: return L10n.text("已就绪", "Ready")
         case .starting: return L10n.text("正在启动", "Starting")
         case .running, .hidden: return L10n.text("Speed 已运行", "Speed active")
+        case .paused: return presentationState.pauseReason?.title ?? L10n.text("Speed 已暂停", "Speed paused")
         case .stopping: return L10n.text("正在关闭", "Stopping")
         case .failed: return L10n.text("需要重新尝试", "Try again")
         }
@@ -626,6 +634,8 @@
             return L10n.text("正在关闭", "Stopping")
         case .running, .hidden:
             return L10n.text("关闭 Speed", "Stop Speed")
+        case .paused:
+            return L10n.text("重新启动悬浮窗", "Restart PiP")
         case .ready, .failed:
             return L10n.text("启动悬浮窗", "Start PiP")
         }
